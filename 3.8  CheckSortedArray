Create Project: Recursion

Create Package: Default

Create class: CheckSorted
-----------------------------------------

public class CheckSorted {

        public static boolean checkSorted(int input[])	{
            if(input.length <= 1)	{ //Base	Case
                return true;
            }

            int smallInput[] = new int[input.length - 1];                        //Recursive	call
            for (int i = 1; i < input.length; i++) {
                smallInput[i - 1] = input[i];              //Recursive	call
            }

            boolean smallAns = checkSorted(smallInput);   //Small	Calculation and	return	statement
            if (!smallAns){
                return false;
            }
            if (input[0] <= input[1]){
                return true;
            } else {
                return false;
            }
        }

        public static void main(String[] args) {
            int input[] = {2,3,3,6,9};
            System.out.println(checkSorted(input));
        }
}
Output:
true
